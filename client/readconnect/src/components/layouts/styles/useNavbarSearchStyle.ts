import { makeStyles } from "@mui/styles";
import colors from "@/utils/theme/base/colors";

const { orange, green, grey } = colors;

export const useNavbarSearchStyle = makeStyles(() => ({
    searchButton: {

  }
}));